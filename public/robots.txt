User-agent: *
Allow: /

# Block truly private areas (auth required, not useful in search)
Disallow: /api/
Disallow: /account/
Disallow: /reports/
Disallow: /campaigns/
Disallow: /admin/

# Note: /login, /signup, /reset-password, /set-password are crawlable so Google
# can refresh their snippets, but each page sets `robots: noindex` in metadata
# so they don't appear in search results.

Sitemap: https://app.rip-tool.com/sitemap.xml
